output("test")

flex result1 = (5 + 3) * 2

flex list1 = [1, 2, 3, 4, 5]

flex element = list1[0]

when result1 > 10:
    output("greater than 10")

output("value1", "value2", "value3")

flex list2 = [10, 20, 30, 40, 50]

function calculate(a, b, c):
    flex data = [a, b, c]
    when data[0] > 0:
        output(data[0])
    return (a + b + c)

flex nested = [[1, 2], [3, 4], [5, 6]]
flex sum = ((10 + 20) + (30 + 40))

function process(x, y, z):
    flex items = [x, y, z]
    when items[0] > 0:
        output((items[0] + items[1]) * items[2])
        return items[0]
