# Heading comment
flex numbers = [1, 2, 3]

function wrap(value):
    output((value))

output((numbers[0]))
flex text = "done"  # Inline trailing comment

/* Block comment spanning
   multiple lines with when true:
       output("inside block")
*/

please kindly maybe

